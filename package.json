{
  "name": "acfun-helper-compresser",
  "version": "1.0.0",
  "description": "compress the js file of acfun-helper",
  "main": "gulpfile.js",
  "keywords": [
    "zip"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp && echo \"命令执行完成之后，只要js文件存在，就可以将其他文件复制到final文件夹中了(请不要覆盖已经压缩好的文件)\" && exit 0"
  },
  "author": "ncb",
  "license": "MPL-2.0",
  "dependencies": {
    "gulp": "^4.0.2",
    "esbuild": "^0.8.2",
    "es6-symbol": "^3.1.3",
    "gulp-esbuild": "^0.2.2"
  }
}